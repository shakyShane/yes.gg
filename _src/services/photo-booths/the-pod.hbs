---
description: >
    The Pod, for adults
cta: true
features: the-pod-features
---

{{inc src="hero.hbs" service=$$.booths.the-pod}}

<section class="wrapper detail">
    <div class="container">

        <div id="demo-test-gallery" class="detail__gallery gallery" data-pswp-uid="1">

            <p class="gallery__hint">Click to zoom</p>
            {{#imgs src="dir:public/content/photo-booths/the-pod/gallery" rel="public"}}
                <a class="gallery__link" href="{{this.large.src}}"
                   data-width="{{this.large.width}}" data-height="{{this.large.height}}">
                    <img src="{{this.thumb.src}}" alt="">
                    <figure></figure>
                </a>
            {{/imgs}}

        </div>
        <div class="detail__content">
            <h2>{{{$$.booths.the-pod.title}}}</h2>
            <p class="lede">{{{$$.booths.the-pod.desc}}}</p>

            <p><strong>From: </strong>£{{$$.booths.the-pod.price.value}}.00</p>
        </div>
    </div>

</section>

{{inc src="features.hbs" feature=$$.booths.the-pod id=page.features}}
