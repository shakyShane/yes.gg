<!DOCTYPE html>
<!--[if gt IE 9]><!--> <html class="no-js ffly" lang="en"> <!--<![endif]-->
<head>
    <!-- META -->
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1" />
    <meta name="description" content="{{page.description }}">
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{page.title}}</title>

    <!-- ICONS -->
    <link href="/logo-ico.ico" rel="shortcut icon" type="image/ico">

    <!-- CSS Includes (PRODUCTION) -->
    <link rel="stylesheet" href="/css/core.css"/>

    {{#if page.og.image}}
        <meta property="og:image" content="{{page.og.image}}"/>
    {{else}}
        <meta property="og:image" content="/img/logo.svg"/>
    {{/if}}

    <style>
        .hero__img {
            display: -webkit-box;
            display: -ms-flexbox;
            display: flex;
            background-position: top center;
            background-repeat: no-repeat;
            background-size: cover;
            height: 0;
            padding-bottom: 150%;
            opacity: 1;
            -webkit-transition: opacity 1s;
            transition: opacity 1s; }

        .hero__img:before {
            content: " ";
            position: absolute;
            display: block;
            width: 100%;
            height: 100%;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            -webkit-transition: background .3s;
            transition: background .3s;
            background: rgba(0, 0, 0, 0.7); }

        @media only screen and (min-width: 370px) {
            .hero__img {
                padding-bottom: 120%; } }

        @media only screen and (min-width: 640px) {
            .hero__img {
                padding-bottom: 60.86%; } }

        @media only screen and (min-width: 640px) {
            .hero__img {
                padding-bottom: 42%; } }

        @media only screen and (min-width: 1160px) {
            .hero__img {
                padding-bottom: 500px;
            }
        }
    </style>

</head>


<body class="page-{{page.path.name}}">
{{inc src="public/img/icons/icons.svg"}}
<main class="page-main">
    {{content}}
</main>

{{inc src="photoswipe.hbs"}}

{{#if_eq env 'dev'}}
<script src="/js/bundle.js"></script>
{{else}}
<script async src="{{$assets.bundle}}"></script>
{{/if_eq}}
<link href="https://fonts.googleapis.com/css?family=Open+Sans:400,600,700" rel="stylesheet">
</body>
