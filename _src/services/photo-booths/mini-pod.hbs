---
description: Our Mini-pod is the perfect choice for kids parties
cta: true
features: mini-pod-features
gallery:  mini-pod-gallery
breadcrumbs:
    - label: Home
      url: /
    - label: Services
      url: /services
    - label: Photo Booths
      url: /services/photo-booths
og:
    image: /content/photo-booths/mini-pod/service-thumb.jpg
---

{{inc src="hero.hbs" service=$$.services.booths-mini-pod}}
{{inc src="breadcrumbs.hbs" items=page.breadcrumbs currentPage=$$.services.booths-mini-pod.title}}

<section class="wrapper detail" id="{{page.gallery}}">
    <div class="container">

        <div id="demo-test-gallery" class="detail__gallery gallery" data-pswp-uid="1">

            <div class="gallery__header">
                <img src="{{$$.services.booths-mini-pod.thumb}}" alt="">
            </div>

            <p class="gallery__hint">Click thumbs to zoom</p>

            {{#imgs src="dir:public/content/photo-booths/mini-pod/gallery" rel="public"}}
                <a class="gallery__link" href="{{this.large.src}}"
                   data-width="{{this.large.width}}" data-height="{{this.large.height}}">
                    <img src="{{this.thumb.src}}" alt="">
                    <figure></figure>
                </a>
            {{/imgs}}
        </div>

        <div class="detail__content">
            <h2>{{{$$.services.booths-mini-pod.title}}}</h2>
            <p class="lede">{{{$$.services.booths-mini-pod.desc}}}</p>
            <p>
                <a data-scroll href="#{{page.features}}" class="button button--alt">Prices and Features</a>
            </p>
        </div>
    </div>
</section>



<section class="wrapper upsell" id="mini-pod-features">
    <div class="container">

        <h1 class="page-title">2 Childrens Booths available</h1>

        <div class="cards cards--feature-list cards--feature-list--two-up" id="the-pod-features">
            <div class="card">
                <p class="card__title">{{$$.services.booths-mini-pod-01.title}}</p>
                <ul class="card__feature-list">
                    {{#each $$.services.booths-mini-pod-01.features}}
                        <li>{{{this}}}</li>
                    {{/each}}
                </ul>
                <div class="card__cta card__cta--bottom">
                    <p class="card__price">
                        <strong>Price: £{{$$.services.booths-mini-pod-01.price.value}}.00</strong>
                    </p>
                    <a href="{{$$.nav.book.url}}" class="button">Book Now</a>
                </div>
            </div>
            <div class="card">
                <p class="card__title">{{$$.services.booths-mini-pod-02.title}}</p>
                <ul class="card__feature-list">
                    {{#each $$.services.booths-mini-pod-02.features}}
                        <li>{{{this}}}</li>
                    {{/each}}
                </ul>
                <div class="card__cta card__cta--bottom">
                    <p class="card__price">
                        <strong>Price: £{{$$.services.booths-mini-pod-02.price.value}}.00</strong>
                    </p>
                    <a href="{{$$.nav.book.url}}" class="button">Book Now</a>
                </div>
            </div>
        </div>
    </div>
</section>
